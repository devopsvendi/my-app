# Jenkins GitHub Maven JAR WAR Demo

This repository contains Maven JAR and WAR projects.

Jenkins checks out the source code from GitHub.

Jenkins performs the Maven build.

Jenkins runs tests.

Jenkins creates the JAR and WAR artifacts.

Jenkins archives the generated artifacts.

Maven target directories are not committed to GitHub.
